# Browser
export BROWSER=zen-browser
export MOZ_ENABLE_WAYLAND=1

# Terminal
export TERMINAL=kitty
export EDITOR=micro
export TERM=xterm-kitty

# Wayland, Qt, Gtk, Electron
export QT_QPA_PLATFORM="wayland;xcb"
export QT_QPA_PLATFORMTHEME="qt6ct"
export QT_SCALE_FACTOR_ROUNDING_POLICY=RoundPreferFloor
export ELECTRON_OZONE_PLATFORM_HINT=wayland
export SDL_VIDEODRIVER=wayland
export _JAVA_AWT_WM_NONREPARENTING=1

# iGPU Primary Renderer
export AQ_DRM_DEVICES=$(for dev in /sys/class/drm/card[0-9]; do [ -e "$dev/device" ] && [ "$(basename "$(readlink -f "$dev/device")")" = "0000:77:00.0" ] && echo "/dev/dri/$(basename "$dev")" && break; done)

# Cursor
export HYPRCURSOR_THEME="Bibata-Modern-Ice"
export HYPRCURSOR_SIZE=20
export XCURSOR_THEME="Bibata-Modern-Ice"
export XCURSOR_SIZE=20
