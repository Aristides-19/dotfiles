# =============================================================================
# ZPROFILE - LOGIN SHELL CONFIGURATION
# Launches Hyprland via UWSM automatically on TTY1 login
# =============================================================================

if [ -z "$DISPLAY" ] && [ "$(tty)" = "/dev/tty1" ]; then
    exec uwsm start hyprland-uwsm.desktop
fi
